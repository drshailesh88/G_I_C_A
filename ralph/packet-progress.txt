# Ralph Packet Progress

## Packet Patterns

<!-- Reusable patterns discovered during packet builds. -->

## Patterns

- DB mock for queries ending in `.where()` (no `.limit()`): add `.then()` and `.catch()` to the chain object so it is thenable. Registration tests use `mockReturnThis()` but that only works when the query ends in `.limit()` or `.orderBy()`.
- `withEventScope` should be mocked in unit tests (`vi.mock('@/lib/db/with-event-scope', ...)`) so Drizzle column references are not needed in tests.
- Upsert pattern: select existing → if found update, else insert. Test the two branches separately with `chainedSelect([])` (no record) vs `chainedSelect([{id:'...'}])`.

## Iteration log

<!-- Dated entries per completed packet build. -->

## 2026-04-19T01:35:00Z — PKT-B-005 — Global cross-event reports

- Story ID: p6-m47-global-reports
- Implemented: Added `getEventsForGlobalReports()` server action to `src/lib/actions/reports.ts` — Super Admin only gate via `session.has({ role: ROLES.SUPER_ADMIN })`; queries all events ordered by startDate desc, returns `{ ok: true, events }` or `{ ok: false, error }`. Added `generateGlobalExport(eventId, type)` server action — Super Admin only; two modes: (1) single-event mode: validates eventId UUID, calls existing `generateExport(eventId, type)` from `lib/exports/excel.ts` for the 6 known types, or runs a dedicated `exportNotificationLog(eventId)` query for the new `notification-log` type; (2) all-events combined mode (`eventId === 'all'`): runs 7 distinct combined query functions that join with the events table to add "Event Name" as the first column (transport-plan generates two sheets: Combined Batches + Combined Passengers; all others generate a single sheet). All combined functions return an ExcelJS Buffer with styled headers and auto-width columns. Returns `{ ok: true, base64: string, filename: string }` — the buffer is encoded as base64 so it can be transferred over a server action boundary. Created server page `src/app/(app)/reports/page.tsx` — checks Super Admin via `auth()`, redirects non-SA to `/dashboard`, fetches event list, passes to client. Created `GlobalReportsClient` client component — event scope dropdown (All Events + individual events by name with status), 7 report type cards with icons/labels/descriptions, download button per card calls `generateGlobalExport` via `useTransition`, decodes base64 and triggers browser download, error banner on failure, link to per-event reports page when a specific event is selected.
- Files changed:
  - src/lib/actions/reports.ts (new — GLOBAL_EXPORT_TYPES registry, assertSuperAdmin, exportNotificationLog, exportAll* combined query functions x6, getEventsForGlobalReports, generateGlobalExport)
  - src/lib/actions/reports.test.ts (new — 13 spec-derived tests: 2 getEventsForGlobalReports + 11 generateGlobalExport covering RBAC x3, validation, single-event x3, all-events x4, error handling)
  - src/app/(app)/reports/page.tsx (new — Super Admin gate + data fetch)
  - src/app/(app)/reports/global-reports-client.tsx (new — GlobalReportsClient with event selector + 7 report cards + base64 download pattern)
- Tests run: `npm run test:run` — 13 new tests pass (reports.test.ts); 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill, travel-cascade).
- Risks/follow-ups: `src/app/(app)/reports/` is in `.gitignore` (matches the `reports/` pattern for playwright output) — files were force-added with `git add -f`. A `.gitignore` exception could be added in a future infra packet. No navigation link to `/reports` was added to the dashboard or sidebar (outside allowed write scope); Super Admin must navigate directly to the URL. The combined "all events" export has no eventId filter — in a multi-tenant setup with multiple organizations this would leak cross-org data; current assumption is single-org deployment consistent with the rest of the app. Notification log export for a specific event uses `eq(notificationLog.eventId, eventId)` without `withEventScope` helper (direct eq filter is equivalent for this scalar equality check). `ExcelJS.Workbook` is not shared across the combined export functions — each call allocates a fresh workbook, which is correct for separate downloads.

## 2026-04-19T00:24:30Z — PKT-B-004 — Event field builder

- Story ID: p6-m51-event-field-builder
- Implemented: Added `STANDARD_TOGGLE_FIELDS`, `CUSTOM_FIELD_TYPES`, `customFieldSchema`, `fieldConfigSchema`, `FieldConfig`, `CustomField` types to `src/lib/validations/event.ts`. Added `updateFieldConfig(eventId, input)` server action to `src/lib/actions/event.ts` — RBAC-gated (super_admin or event_coordinator only; ops and read_only rejected), validates eventId UUID, validates fieldConfig via `fieldConfigSchema` (standardFields map + customFields array ≤10), then calls `assertEventAccess` and updates `events.fieldConfig` JSONB. Updated `getEventBySlug` to include `fieldConfig` in its select projection so public registration page can access it. Created server page at `/events/[eventId]/fields/page.tsx` — auth + coordinator/super_admin canWrite gate, loads event, passes to `FieldBuilderClient`. Created `FieldBuilderClient` — standard fields section with always-on rows (fullName/email/phone) and toggle switches for designation/specialty/organization/city/age; custom fields section with per-field cards showing label input, type dropdown (text/number/select/date/file), required checkbox, options textarea (for select type), remove button; Add Field button (disabled at 10); Save button calls `updateFieldConfig`. Updated public `/e/[eventSlug]/register/page.tsx` to pass `fieldConfig` prop to `RegistrationFormClient`. Rewrote `RegistrationFormClient` to render dynamic fields: fullName/email/phone always shown; standard toggle fields conditionally rendered; custom fields appended at end (select renders `<select>`, file renders `<input type="file">`, others use appropriate `<input type>`); custom field values collected into `preferences` object on submit (stored as `custom_{id}` keys in `preferencesJson` JSONB — existing registrations unaffected since preferences schema accepts arbitrary plain objects).
- Files changed:
  - src/lib/validations/event.ts (added STANDARD_TOGGLE_FIELDS, CUSTOM_FIELD_TYPES, customFieldSchema, fieldConfigSchema, FieldConfig, CustomField exports)
  - src/lib/actions/event.ts (added fieldConfigSchema import + FieldConfig type import; added fieldConfig to getEventBySlug select; added UpdateFieldConfigResult type + updateFieldConfig action)
  - src/lib/actions/event-field-builder.test.ts (new — 19 spec-derived tests: 5 RBAC + 6 validation + 2 standard field toggles + 3 custom fields + 3 side effects)
  - src/app/(app)/events/[eventId]/fields/page.tsx (new — server page, auth + canWrite gate)
  - src/app/(app)/events/[eventId]/fields/field-builder-client.tsx (new — FieldBuilderClient + CustomFieldCard)
  - src/app/(app)/events/[eventId]/fields/field-builder.test.ts (new — 21 spec-derived tests: 4 parseFieldConfig default + 2 custom passthrough + 4 visibility logic + 2 max count + 6 type coverage + 2 data integrity + 1 scope)
  - src/app/(public)/e/[eventSlug]/register/page.tsx (added fieldConfig prop pass to RegistrationFormClient)
  - src/app/(public)/e/[eventSlug]/register/registration-form-client.tsx (rewrote to accept fieldConfig, render dynamic standard + custom fields, collect custom values into preferences)
- Tests run: `npm run test:run` — 19 new tests pass (event-field-builder.test.ts); 21 new tests pass (field-builder.test.ts); 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill, travel-cascade).
- Risks/follow-ups: File type custom fields render `<input type="file">` in the public form but the file content is not uploaded — only the filename string is stored in preferences. Full file upload to R2 with a presigned URL flow is deferred. Custom field ID generation uses `crypto.randomUUID()` client-side in the field builder — UUIDs are generated at add-field time and persisted with the config, so they remain stable across saves. Standard field toggles affect only the UI — `publicRegistrationSchema` keeps all standard fields optional, so existing registrations with those fields stored remain valid when a field is later toggled off.

## 2026-04-18T18:55:00Z — PKT-B-003 — Duplicate event structure

- Story ID: p6-event-duplicate
- Implemented: Added `duplicateEvent(sourceEventId, { name, newStartDate })` server action to `src/lib/actions/event.ts` — RBAC-gated (canCreateEvents: super_admin or event_coordinator), validates sourceEventId UUID + input via `duplicateEventSchema`. Loads source event, calculates `shiftMs = newStart - srcStart`, preserves event duration for new endDate. Creates new event (status='draft') with branding/moduleToggles/fieldConfig/communicationSettings copied from source. Deep-copies: `halls` (builds old→new ID map), `sessions` (two-pass for parent-child hierarchy — parents first to build map, then children with mapped parentSessionId; all dates shifted by `shiftMs`; status reset to 'draft'), `sessionRoleRequirements` (mapped sessionIds), `notificationTemplates` (event-specific only — `isSystemTemplate=false` filter; status='draft', versionNo=1), `automationTriggers` (templateId mapped to new IDs; triggers that reference global system templates keep original templateId). Assigns duplicating user as event owner via `eventUserAssignments`. Created server page `/events/[eventId]/duplicate/page.tsx` — auth gate + RBAC check (super_admin or coordinator), loads source event, passes to client. Created `DuplicateEventClient` — form with pre-filled name ("Copy of {source name}") and new start date; shows source event info and what IS/ISN'T copied; displays calculated new end date preview; submit calls `duplicateEvent`, redirects to new event workspace on success. Added "Duplicate Event" tile (Copy icon) to ADMIN section of `event-workspace-client.tsx`. Added `halls, sessions, sessionRoleRequirements, notificationTemplates, automationTriggers` imports + `z`, `inArray` imports to event.ts.
- Files changed:
  - src/lib/actions/event.ts (added halls/sessions/sessionRoleRequirements/notificationTemplates/automationTriggers/z/inArray imports; added shiftDate helper; added duplicateEventSchema, DuplicateEventResult type, duplicateEvent action)
  - src/lib/actions/event-duplicate.test.ts (new — 25 spec-derived tests: 3 RBAC + 4 validation + 5 new event properties + 3 date shifting + 4 structural copy + 3 templates/triggers + 1 person-linked exclusion + 2 owner assignment)
  - src/app/(app)/events/[eventId]/duplicate/page.tsx (new — server page, auth + RBAC gate)
  - src/app/(app)/events/[eventId]/duplicate/duplicate-event-client.tsx (new — DuplicateEventClient form with name, newStartDate, end date preview)
  - src/app/(app)/events/[eventId]/event-workspace-client.tsx (added Copy import + Duplicate Event tile in ADMIN section)
- Tests run: `npm run test:run` — 25 new tests pass (event-duplicate.test.ts); 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill, travel-cascade).
- Risks/follow-ups: Mock order in tests is critical — `duplicateEvent` loads the source event BEFORE calling `getOrCreateDefaultOrg`, so the select mocks must be queued: source event (where+limit) first, then org (limit). Child sessions (parentSessionId != null) are handled in a second INSERT pass; if the source has no child sessions (default fixture), that insert is skipped. Branding image storage keys (`logoStorageKey`, `headerImageStorageKey`) inside the copied `branding` JSONB still reference the source event's R2 paths — future enhancement should re-key the images under the new eventId. `registrationSettings` is copied as-is (structural config, not operational data); callers should review capacity settings before publishing the duplicate.

## 2026-04-18T17:52:00Z — PKT-B-002 — Attendance report drill-down page

- Story ID: p5-m58-attendance-report-page
- Implemented: Added `getAttendanceReportData(eventId)` server action to `attendance.ts` — validates UUID, gates access via `assertEventAccess`, runs 6 queries inside a `repeatable read` transaction: (1) confirmed registration count, (2) total attendance count, (3) by-method group, (4) by-category group (left join to handle walk-ins), (5) by-day group using `AT TIME ZONE 'Asia/Kolkata'` cast, (6) by-session group inner-joined with `sessions` table using `isNotNull` filter. Returns `AttendanceReportData` with `overall` (totalRegistrations, totalCheckedIn, checkInRate, byMethod, byCategory), `byDay` (date string, count, percentage), `bySession` (sessionId, title, sessionDate ISO, startAtUtc, endAtUtc, count, percentage). Results sorted in JS by date/time. Added new types: `AttendanceReportOverall`, `AttendanceReportDay`, `AttendanceReportSession`, `AttendanceReportData`. Created `/events/[eventId]/qr/report` server page — auth gate + `getAttendanceReportData` fetch, passes to client. Created `AttendanceReportClient` — three-tab UI (Overall | By Day | By Session); Overall tab shows Total Registered / Checked In / Rate stat cards, By Method table, By Category table; By Day tab shows date/count/% table; By Session tab shows session title, IST date+time, count, % table; Export Excel button reuses existing `/api/events/${eventId}/exports/attendance-report` endpoint via fetch+blob pattern; back-link to QR Check-In. Updated `qr-checkin-client.tsx` to import `Link` and add "View Full Report →" link in the statistics panel (below method breakdown), satisfying requirement 1 (drill-down reachable from attendance stat cards). 12 spec-derived tests in `attendance-report.test.ts` cover: UUID validation, RBAC propagation, transaction isolation level, totalRegistrations shape, totalCheckedIn shape, checkInRate calculation, divide-by-zero safety, byMethod shape, byCategory null filtering, byDay date+percentage, bySession metadata+percentage, zero-percentage edge case.
- Files changed:
  - src/lib/actions/attendance.ts (added sessions import, isNotNull import; added AttendanceReportOverall/Day/Session/Data types; added getAttendanceReportData action)
  - src/lib/actions/attendance-report.test.ts (new — 12 spec-derived tests, all pass)
  - src/app/(app)/events/[eventId]/qr/report/page.tsx (new — server page, auth gate + data fetch)
  - src/app/(app)/events/[eventId]/qr/report/attendance-report-client.tsx (new — AttendanceReportClient with 3-tab UI + export)
  - src/app/(app)/events/[eventId]/qr/qr-checkin-client.tsx (added Link import + View Full Report link in stats panel)
- Tests run: `npm run test:run` — 12 new tests pass (attendance-report.test.ts); 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill, travel-cascade).
- Risks/follow-ups: `bySession` only shows sessions that have at least one check-in record — sessions with 0 check-ins are not shown; a future enhancement could include all scheduled sessions with a 0 count. The Export Excel button reuses the existing `attendance-report` export type which outputs all check-in records in a flat list; a multi-sheet export (one per day/session) is deferred. `startAtUtc`/`endAtUtc` in the client are cast via `new Date(row.startAtUtc as unknown as string)` because Next.js serializes Date objects to ISO strings during RSC→client prop transfer.

## 2026-04-18T17:30:00Z — PKT-B-001 — Suggested transport batches

- Story ID: p3-transport-suggest-batches
- Implemented: Added `floorToThreeHourWindowUtc()` helper and `buildClusters()` pure function that groups unassigned travel records by (movementType, city, 3-hour UTC window) into `TravelCluster` objects. Added `generateTransportSuggestions(eventId)` — UUID-gated, queries active travel records minus already-assigned ones, clusters them, skips existing auto-batch keys (idempotent), inserts `transportBatch` rows with `batchSource='auto'` and their passenger pre-assignments. Added `refreshTransportSuggestions(eventId)` (write-gated wrapper for manual Ops trigger). Added `getSuggestedBatches(eventId)` — returns auto non-cancelled batches with joined passenger names. Added `acceptSuggestion(eventId, batchId)` — flips `batchSource` from `auto` to `manual` so it joins the planned flow. Added `discardSuggestion(eventId, batchId)` — cancels all passenger assignments then cancels the batch. Added `mergeSuggestions(eventId, keepBatchId, discardBatchId)` — re-points passengers from discard to keep, cancels discard batch. Added `splitSuggestion(eventId, batchId, passengerAssignmentIds[])` — creates a new auto batch with same metadata, moves selected passengers, leaves at least one in the original. All mutations write to audit log. Updated `transport/page.tsx` to fetch suggestions in parallel with manual batches. Updated `TransportPlanningClient` to accept `suggestions` prop and render `<SuggestionsPanel>`. Created `SuggestionsPanel` client component — amber-toned section separate from planned batches; cards show route, movement badge, date, time window, city, passenger pills; Accept/Discard buttons; Merge flow (button + select + confirm); Split flow (toggle passenger pills + confirm); Refresh button; per-batch and global error states; all actions use dynamic imports + `router.refresh()`. Registered `registerTransportCascadeHandlers` in `handlers/index.ts`; handler listens to TRAVEL_SAVED and TRAVEL_CREATED, calls `generateTransportSuggestions`, swallows errors via `captureCascadeError`. Updated `CASCADE_DIRECTION` in `cascade/events.ts` to add `transport_suggestion_refresh` downstream of `travel.created` and `travel.saved`.
- Files changed:
  - src/lib/actions/transport.ts (added floorToThreeHourWindowUtc, buildClusters, SuggestedBatch type, generateTransportSuggestions, refreshTransportSuggestions, getSuggestedBatches, acceptSuggestion, discardSuggestion, mergeSuggestions, splitSuggestion; added inArray import)
  - src/lib/actions/transport-suggestions.test.ts (new — 30 spec-derived tests: 10 buildClusters + 4 floorToThreeHourWindowUtc + 4 generateTransportSuggestions + 2 getSuggestedBatches + 4 acceptSuggestion + 2 discardSuggestion + 2 mergeSuggestions + 1 splitSuggestion + 1 more)
  - src/app/(app)/events/[eventId]/transport/page.tsx (added getSuggestedBatches parallel fetch, suggestions prop pass)
  - src/app/(app)/events/[eventId]/transport/transport-planning-client.tsx (added SuggestedBatch import, SuggestionsPanel import, suggestions prop, render SuggestionsPanel)
  - src/app/(app)/events/[eventId]/transport/transport-planning-client.test.tsx (added suggestions: [] to createElement calls)
  - src/app/(app)/events/[eventId]/transport/suggestions-panel.tsx (new — SuggestionsPanel client component)
  - src/app/(app)/events/[eventId]/transport/suggestions-panel.test.tsx (new — 12 spec-derived tests)
  - src/lib/cascade/handlers/transport-cascade.ts (new — registerTransportCascadeHandlers, runTransportSuggestionRefresh)
  - src/lib/cascade/handlers/transport-cascade.test.ts (new — 5 handler tests)
  - src/lib/cascade/handlers/index.ts (added registerTransportCascadeHandlers import + call)
  - src/lib/cascade/events.ts (added transport_suggestion_refresh to travel.created and travel.saved CASCADE_DIRECTION entries)
- Tests run: `npm run test:run` — 30 new tests pass (transport-suggestions.test.ts); 12 new tests pass (suggestions-panel.test.tsx); 5 new tests pass (transport-cascade.test.ts); pre-existing 67+61 transport tests remain green; 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill, travel-cascade).
- Risks/follow-ups: Suggestions do not auto-refresh on the transport page in real-time — they refresh on page load and on manual Refresh button. For inbound records, `pickupHub` is set to `toLocation` (terminal/gate) which may be a terse code like "BOM T2"; a future packet could do a lookup against a hub registry. Merge UI lets Ops select any suggestion as target; if movementType/city differ between the two suggestions being merged, the resulting batch has mixed passengers (not validated by the server — domain rule says batches can be heterogeneous after manual edits). Split UI shows raw passenger-assignment IDs only when personName is null.

## 2026-04-18T16:35:00Z — PKT-A-015 — People change history backed by audit log

- Story ID: p2-people-change-history
- Implemented: Added `writeAudit` calls to `createPerson`, `updatePerson`, `archivePerson`, `restorePerson`, and `anonymizePerson` in `person.ts`, each writing a `resource='people'` row with `changedFields` in meta. Added `getPersonHistory(personId, page)` server action — RBAC-gated (PEOPLE_READ_ROLES; OPS is forbidden), validates personId UUID, queries `auditLog` where `resource='people' AND resourceId=personId`; Super Admin sees all rows, others see `eventId IS NULL` rows only to prevent cross-event leakage. Ordered newest-first, paginated at 25 per page, returns `{ rows, total, page, totalPages }`. Updated `PersonDetailPage` (server page) to fetch initial history in parallel with person and pass as `initialHistory` prop. Updated `PersonDetailClient` to accept `initialHistory?: PersonHistoryResult` (defaults to empty), replace the simple createdAt/updatedAt section with a full paginated audit history list showing actor (truncated Clerk ID), action label (Create/Update/Archived/Restored/Anonymized/Merged), IST timestamp, and changed-field tags. Prev/Next pagination calls `getPersonHistory` as a server action on click. Added `writeAudit` mock to `person.test.ts` and `person.mutation-kill.test.ts` to prevent db.insert conflicts from new audit calls. Updated `person-detail-client.test.tsx` mock to include `getPersonHistory`.
- Files changed:
  - src/lib/actions/person.ts (added auditLog schema import; writeAudit calls in createPerson/updatePerson/archivePerson/restorePerson/anonymizePerson; added HISTORY_PAGE_SIZE const, PersonHistoryRow type, PersonHistoryResult type, getPersonHistory action)
  - src/lib/actions/person-history.test.ts (new — 12 spec-derived tests: 4 RBAC + 1 validation + 3 data shape + 3 pagination + 1 meta passthrough)
  - src/lib/actions/person.test.ts (added vi.mock('@/lib/audit/write', ...) to prevent insert-chain conflicts)
  - src/lib/actions/person.mutation-kill.test.ts (added same writeAudit mock)
  - src/app/(app)/people/[personId]/page.tsx (added getPersonHistory parallel fetch, EMPTY_HISTORY constant, initialHistory prop pass)
  - src/app/(app)/people/[personId]/person-detail-client.tsx (added history state/pagination, HistoryRow component, ACTION_LABEL map, Clock+ChevronLeft+ChevronRight icons; replaced simple date display with full audit list)
  - src/app/(app)/people/[personId]/person-detail-client.test.tsx (added getPersonHistory: vi.fn() to mock)
- Tests run: `npm run test:run` — 12 new tests pass (person-history.test.ts); 197 pre-existing tests in person.test.ts + person.mutation-kill.test.ts pass; 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill, travel-cascade).
- Risks/follow-ups: Actor is shown as a truncated Clerk user ID — a future packet can resolve it to a display name via Clerk Users API. The `getPersonHistory` server action is called from the client for pagination; on the initial load it's fetched server-side (no waterfall). OPS role cannot read person history even though it can read travel/accommodation records for the same person; consistent with the people module RBAC boundary. `vi.clearAllMocks()` (not `resetAllMocks()`) in person.test.ts preserves mockReturnValue implementations across tests within a describe block — the writeAudit mock fully prevents this ambiguity.

## 2026-04-18T15:52:00Z — PKT-A-014 — Merge duplicate people UI

- Story ID: p2-people-merge-duplicates-ui
- Implemented: Added `mergePeopleSchema`, `MergePeopleResult` type, and `mergePeople(input)` server action to `src/lib/actions/person.ts`. Action: RBAC-gated (write roles only), validates keepId/dropId as UUIDs, rejects self-merge, fetches both people, runs atomic DB transaction that (1) re-points `event_people` (deletes conflicts by eventId, then updates), (2) re-points `event_registrations` (same conflict pattern), (3) re-points `session_assignments` (loops over keeper's sessionId+role pairs to delete conflicts, then updates), (4) updates `faculty_invites`, `travelRecords`, `accommodationRecords`, `transportPassengerAssignments` directly, (5) updates winner with merged field values (left/right/both choices; tags always unioned), (6) soft-archives loser with `archivedAt`/`archivedBy`. Tombstone metadata (mergedIntoId, fieldChoices) written to audit log. Created `/people/merge/page.tsx` server page — auth + write-role gate, reads `?a` and `?b` UUID params, fetches both people via `getPerson`, passes to `MergeClient`. Created `MergeClient` — three states: (1) no personA: instruction page; (2) personA only: person card + UUID input to pick person B; (3) both: side-by-side comparison table with per-field Keep Left / Keep Right / Both buttons, warning banner, Confirm Merge button, success state with link to survivor. Added "Merge" button (GitMerge icon) to people list header and "Merge" link to person detail page header as entrypoints.
- Files changed:
  - src/lib/actions/person.ts (added eventRegistrations, sessionAssignments, facultyInvites, travelRecords, accommodationRecords, transportPassengerAssignments imports; added inArray import; added mergePeopleSchema, MergePeopleResult, pickTextField, mergePeople)
  - src/lib/actions/person-merge.test.ts (new — 22 spec-derived tests: 4 RBAC + 5 validation + 5 field choices + 5 transaction integrity + 2 audit log + 1 return value)
  - src/app/(app)/people/merge/page.tsx (new — server page, auth gate + parallel getPerson fetch)
  - src/app/(app)/people/merge/merge-client.tsx (new — MergeClient with 3 states: NoPeople, PickSecond, Comparison)
  - src/app/(app)/people/people-list-client.tsx (added GitMerge import + Merge button in header)
  - src/app/(app)/people/[personId]/person-detail-client.tsx (added GitMerge import + Merge link in actions)
- Tests run: `npm run test:run` — 22 new tests pass; 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill, travel-cascade).
- Risks/follow-ups: `git stash pop` conflicted with tsconfig.tsbuildinfo during pre-existing baseline check — stash was discarded and all changes re-applied manually. Conflict detection for session_assignments loops over keeper's pairs (N+1 deletes per pair); acceptable for the rare merge operation. The "Find second person" step uses a bare UUID input — a search-based picker with autocomplete is deferred (not in spec scope). The merge page entry point from the people list links to /people/merge (no person preselected); from the person detail page, it links to /people/merge?a=personId (person A preselected).

## 2026-04-18T15:10:00Z — PKT-A-013 — Transfer event ownership workflow

- Story ID: p1-transfer-event-ownership
- Implemented: Added `transferEventOwnership(eventId, newOwnerUserId)` server action to `src/lib/actions/event.ts` — asserts Super Admin via `session.has`, validates eventId UUID + non-empty newOwnerUserId, deactivates all active `owner`-type assignments for the event (collaborator assignments untouched), then inserts a new owner record or reactivates an existing one for the new owner, then updates `events.updatedBy`/`updatedAt` to reflect the actor. Returns `{ ok: true }` or `{ ok: false; error: string }`. Added `TransferOwnershipResult` export type. Created server page at `/events/[eventId]/transfer-ownership/page.tsx` — super-admin gate (redirect to /dashboard if not SA), loads event + assignments + teamMembers in parallel, resolves current owner display name. Created `TransferOwnershipClient` — shows current owner card, candidate dropdown (excludes current owner), confirm button with transition, success state with back-to-team link, inline error feedback. Added "Transfer Ownership" tile (ArrowRightLeft icon) to ADMIN section in `event-workspace-client.tsx`.
- Files changed:
  - src/lib/actions/event.ts (added ROLES import; added TransferOwnershipResult type + transferEventOwnership action)
  - src/lib/actions/event-transfer-ownership.test.ts (new — 13 spec-derived tests: 3 RBAC + 3 validation + 7 ownership mutation)
  - src/app/(app)/events/[eventId]/transfer-ownership/page.tsx (new — server page, super-admin gate + parallel data fetch)
  - src/app/(app)/events/[eventId]/transfer-ownership/transfer-ownership-client.tsx (new — TransferOwnershipClient)
  - src/app/(app)/events/[eventId]/event-workspace-client.tsx (added ArrowRightLeft import + Transfer Ownership tile in ADMIN section)
- Tests run: `npm run test:run` — 13 new tests pass; 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill, travel-cascade).
- Risks/follow-ups: Transferring ownership to the currently active owner is allowed (no-op from DB perspective — deactivate then reactivate with same userId). No guard prevents transfer to a Clerk user who has no event access yet (they'd get owner assignment added). The workspace "Transfer Ownership" tile is visible to all roles, but the page and action gate on Super Admin — non-SA users who navigate directly see a dashboard redirect.

## 2026-04-18T14:46:00Z — PKT-A-011 — Revised responsibility notifications on publish

- Story ID: p2-program-revised-responsibility-notifications
- Implemented: Added `ProgramVersionPublishedPayload` type and `PROGRAM_VERSION_PUBLISHED: 'conference/program.version_published'` event to `cascade/events.ts`. Added `programVersionPublishedPayloadSchema` Zod schema to `cascade/emit.ts` and registered it in `productionPayloadSchemas`. Created `src/lib/cascade/handlers/program-cascade.ts` with `handleProgramVersionPublished` — skips if no baseVersionId (first publish), fetches both version snapshots from DB, computes faculty-specific added/removed session sets by filtering assignments per personId, skips faculty with zero net changes, sends `program_update` email notification per changed faculty via `sendNotification` with idempotency key `notify:program-version:<versionId>:<personId>:email`. Registered `registerProgramCascadeHandlers` in `cascade/handlers/index.ts`. Added `emitCascadeEvent` call in `publishProgramVersion` (program.ts) after successful DB insert — passes versionId, versionNo, baseVersionId, affectedPersonIds in payload.
- Files changed:
  - src/lib/cascade/events.ts (ProgramVersionPublishedPayload type, PROGRAM_VERSION_PUBLISHED event, CASCADE_DIRECTION entry)
  - src/lib/cascade/emit.ts (programVersionPublishedPayloadSchema, registered in productionPayloadSchemas)
  - src/lib/cascade/handlers/program-cascade.ts (new — handleProgramVersionPublished, registerProgramCascadeHandlers)
  - src/lib/cascade/handlers/index.ts (import + registerProgramCascadeHandlers() call)
  - src/lib/actions/program.ts (emitCascadeEvent import + CASCADE_EVENTS import + emit call in publishProgramVersion)
  - src/lib/cascade/handlers/program-cascade.test.ts (new — 11 spec-derived handler tests)
  - src/lib/actions/program-version-publish-emit.test.ts (new — 4 action emit tests)
- Tests run: `npm run test:run` — 15 new tests pass (11 handler + 4 action); 8 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, full-journey, mutation-kill-2, notifications.adversarial, notifications.mutation-kill).
- Risks/follow-ups: Pattern fix documented — `vi.clearAllMocks()` does NOT clear `mockReturnValueOnce` queues; use `vi.resetAllMocks()` + re-apply implementations in `beforeEach` for tests with multiple sequential DB mock calls. Faculty diff uses `sessionId` from snapshot assignments to detect changes; if the same faculty is reassigned to the same session (same sessionId, different role), no notification is sent (role changes are not diffed). WhatsApp channel notification for program diffs is not included in this packet (spec says email only); PKT-A-012 covers the aggregated bundle.

## 2026-04-18T14:10:00Z — PKT-A-007 — Communications hub page

- Story ID: p4-m13-template-list-hub
- Implemented: Added two new server actions to `notifications.ts` — `getTemplatesHub(input)` calls `listTemplatesForEvent` with eventId + optional channel filter, asserts COMMUNICATIONS_READ_ROLES (OPS forbidden); `getNotificationLog(input)` queries `notificationLog` for ALL statuses (not only failed), applies optional channel/status filters, paginates with limit/offset. Created server page at `/events/[eventId]/templates/page.tsx` — reads searchParams for tab/channel/status/offset, fetches templates + log in parallel, passes to client. Created `TemplatesHubClient` — two-tab layout (Templates | Delivery Log); Templates tab shows channel-grouped template cards (email section, whatsapp section) with status badges and links to `/events/[eventId]/templates/[id]/edit`; global (system) templates marked with "Global" badge; channel filter pills; Delivery Log tab shows all send statuses with recipient/template/channel/status/IST timestamp, channel + status filter pills, and Prev/Next pagination.
- Files changed:
  - src/lib/actions/notifications.ts (added listTemplatesForEvent + db/drizzle imports; added getTemplatesHub and getNotificationLog server actions)
  - src/app/(app)/events/[eventId]/templates/page.tsx (new — server page, auth + parallel data fetch)
  - src/app/(app)/events/[eventId]/templates/templates-hub-client.tsx (new — TemplatesHubClient with two-tab UI)
  - src/app/(app)/events/[eventId]/templates/templates-hub.test.ts (new — 27 spec-derived tests: 5 getTemplatesHub + 8 getNotificationLog + 14 client)
- Tests run: `npm run test:run` — 27 new tests pass; 5 pre-existing failing files unchanged (certificate-issuance, archive-bypass, full-journey, mutation-kill-2).
- Risks/follow-ups: Template editor route `/events/[eventId]/templates/[id]/edit` is not built yet (PKT-A-008 depends on this packet). The "Communications" tile in the workspace already points to `/templates` (already wired). The Delivery Log tab fetches up to 50 rows per page; recipients without a person join are not name-resolved (only email/phone shown). IST timestamps use `toLocaleString` with `Asia/Kolkata`.

## 2026-04-18T13:35:00Z — PKT-A-006 — Public attendee-facing scientific program route

- Story ID: p2-m04-public-attendee-program
- Implemented: Added `getPublicProgramData(eventId)` server action to `program.ts` — validates UUID, checks for at least one published program version (returns `hasPublishedVersion: false` if none), then queries live public non-cancelled sessions with hall and speaker name joins from people table, builds parent/child session tree, returns `{ sessions, halls, hasPublishedVersion }`. Exported `PublicProgramData` type alias. Created server page at `/e/[eventSlug]/program/page.tsx` — calls `getEventBySlug` (404 for draft/missing events), calls `getPublicProgramData`, reads `?d=YYYY-MM-DD` from searchParams for date deep-linking. Created `PublicProgramClient` client component — shows "not published" state when `hasPublishedVersion = false`, shows "empty" state when sessions is empty, otherwise shows date tabs (URL-synced via `useRouter.replace`), mobile card list (`md:hidden`) with session type badge/time/hall/speakers/child sessions, and desktop pixel-positioned time-axis schedule grid (`hidden md:block`) with halls as columns and hour grid lines.
- Files changed:
  - src/lib/actions/program.ts (added getPublicProgramData, PublicProgramSession type, PublicProgramData export)
  - src/app/(public)/e/[eventSlug]/program/page.tsx (new — server page)
  - src/app/(public)/e/[eventSlug]/program/program-client.tsx (new — PublicProgramClient with mobile card + desktop grid views)
  - src/app/(public)/e/[eventSlug]/program/program.test.ts (new — 12 spec-derived tests for getPublicProgramData)
- Tests run: `npm run test:run` — 12 new tests pass; 6 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, travel-cascade, full-journey, mutation-kill-2).
- Risks/follow-ups: Desktop grid positions sessions using UTC+5:30 fixed offset for IST conversion — does not handle DST (IST has no DST, so this is correct). Sessions without `startAtUtc`/`endAtUtc` are rendered in the mobile list but not positioned in the desktop grid (they would overlap at top). No link from the event landing page to the program page — attendees must navigate to `/e/[eventSlug]/program` directly or via shared link. Photo/avatar not shown in program view (not in spec scope).

## 2026-04-18T11:35:00Z — PKT-C-003 — Preview revised emails modal

- Story ID: p2-d1-preview-revised-emails
- Implemented: Added three server actions to `program.ts` — `getVersionPreviewData(eventId, versionId)` (returns version, eventName, affectedFaculty list), `getVersionEmailParts(eventId, versionId, personId)` (interpolates program_update template body split around `{{changesSummary}}`, returns subject/bodyBefore/bodyAfter/changesSummaryJson/recipientEmail), and `sendVersionEmails(eventId, versionId)` (sends program_update email per affected person via notification service, updates version notificationStatus). Created `PreviewEmailsModal` client component — full-screen sheet on mobile (fixed inset-0), centered 560px modal on desktop (max-w-[560px]). Modal opens from "Preview Emails" button on version cards (only shown when affectedPersonIdsJson is non-empty). Shows faculty dropdown ("Preview as"), "X of N affected" counter, rendered email preview with To/Subject header and color-coded diff blocks (green = added sessions, red = removed sessions), "Close" and "Send All" footer buttons, and "N faculty will receive this email" count. Extended `ProgramVersion` type to include `affectedPersonIdsJson`. Updated `changes.test.ts` with mock for new program actions.
- Files changed:
  - src/lib/actions/program.ts (added inArray/interpolate imports; added buildChangesSummaryText helper; added getVersionPreviewData, getVersionEmailParts, sendVersionEmails server actions)
  - src/app/(app)/events/[eventId]/changes/preview-emails-modal.tsx (new — PreviewEmailsModal client component)
  - src/app/(app)/events/[eventId]/changes/preview-emails-modal.test.ts (new — 21 tests: 4 getVersionPreviewData + 7 getVersionEmailParts + 4 sendVersionEmails + 6 modal structure)
  - src/app/(app)/events/[eventId]/changes/changes-client.tsx (added useState, PreviewEmailsModal import, affectedPersonIdsJson to ProgramVersion type, "Preview Emails" button in VersionCard, modal wiring)
  - src/app/(app)/events/[eventId]/changes/changes.test.ts (added vi.mock for @/lib/actions/program; added affectedPersonIdsJson to baseVersion fixture)
- Tests run: `npm run test:run` — 38 new tests pass (21 new + 17 pre-existing in changes.test.ts); 6 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, travel-cascade, full-journey, mutation-kill-2).
- Risks/follow-ups: Template body is hardcoded from system-templates seed rather than resolved via renderTemplate DB lookup — event-specific template overrides will not be reflected in preview. Upgrade to DB-resolved templates in a future packet. Diff blocks show session counts ("+2 sessions added") not individual session titles (titles require snapshot join — deferred). The `sendVersionEmails` dynamic import of `@/lib/notifications/send` is used to avoid polluting program.ts bundle, but static import would also work.

## 2026-04-18T10:01:45Z — PKT-A-010 — Program version history page

- Story ID: p2-m52-version-history
- Implemented: Created `/events/[eventId]/changes` route. Server page (`page.tsx`) authenticates via Clerk, calls existing `getProgramVersions(eventId)` action, passes result to `ChangesClient`. Client component renders versions newest-first (backend returns desc order). Each version card shows: version number, publishedAt in IST, structured change counts from `changesSummaryJson` (added_sessions count, removed_sessions count, total_sessions, total_assignments), `changesDescription` and `publishReason` when non-null. Graceful empty state when no versions exist. The existing "Changes" tile in event-workspace-client already links to `/changes`, so no workspace file change needed.
- Files changed:
  - src/app/(app)/events/[eventId]/changes/page.tsx (new — server page, auth + getProgramVersions)
  - src/app/(app)/events/[eventId]/changes/changes-client.tsx (new — ChangesClient with VersionCard)
  - src/app/(app)/events/[eventId]/changes/changes.test.ts (new — 16 spec-derived tests, all pass)
- Tests run: `npm run test:run` — 16 new tests pass; 6 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, travel-cascade, full-journey, mutation-kill-2).
- Risks/follow-ups: publishedBy displays the raw Clerk user ID — a future packet may resolve it to a display name. The "Changes" tile always shows in workspace regardless of module toggle (key: null), consistent with existing behaviour. PKT-C-003 (preview revised emails) depends on this page and can now be unblocked.

## 2026-04-18T11:20:00Z — PKT-C-002 — Speaker profile public view

- Story ID: p2-d5-speaker-profile
- Implemented: Added `bio` and `photoStorageKey` schema fields (migration already present in git working tree). Created `getPublicSpeakers(eventId)` server action (`src/lib/actions/speaker-profile.ts`) — queries public non-cancelled sessions, joins sessionAssignments with people, deduplicates by personId, returns each speaker with their event-scoped session list including hallName. Updated `src/app/(public)/e/[eventSlug]/page.tsx` to call `getPublicSpeakers` and pass result to `EventLandingClient`. Updated `event-landing-client.tsx` to add "Featured Speakers" section with speaker cards (avatar placeholder, name, designation, org) that open `SpeakerProfileOverlay` on click. Overlay is a full-height bottom sheet on mobile (`bottom-0 rounded-t-2xl`) and a two-column centered modal on desktop (`md:w-[640px] md:grid-cols-[200px_1fr]`), showing photo placeholder, identity, About section, and Sessions at this event list with role badges.
- Files changed:
  - src/lib/actions/speaker-profile.ts (new — getPublicSpeakers server action)
  - src/lib/actions/speaker-profile.test.ts (new — 8 tests, all pass)
  - src/app/(public)/e/[eventSlug]/speaker-profile-overlay.tsx (new — SpeakerProfileOverlay client component)
  - src/app/(public)/e/[eventSlug]/speaker-profile-overlay.test.tsx (new — 17 tests, all pass)
  - src/app/(public)/e/[eventSlug]/page.tsx (added getPublicSpeakers fetch, speakers prop)
  - src/app/(public)/e/[eventSlug]/event-landing-client.tsx (added useState, Featured Speakers section, SpeakerProfileOverlay wiring)
  - src/lib/db/schema/people.ts (bio + photoStorageKey fields — already in working tree)
  - drizzle/migrations/0008_add_people_bio_and_photo.sql (already in working tree)
- Tests run: `npm run test:run` — 26 new tests pass (8 action + 17 overlay + 1 spec); 6 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, travel-cascade, full-journey, mutation-kill-2).
- Risks/follow-ups: Photo placeholder used for all speakers — actual R2 signed URL generation for `photoStorageKey` deferred (requires authenticated infrastructure not yet wired to public route). The packet spec says "photo placeholder" in the wireframe, consistent with v1. No signed URLs generated.

## 2026-04-18T10:47:00Z — PKT-C-006 — Ops resend logistics notification actions

- Story ID: p3-ops-resend-logistics-notification
- Implemented: Added `···` ellipsis DropdownMenu to every non-cancelled travel and accommodation row/card. Clicking "Resend Notification" opens a `ResendNotificationDialog` — bottom sheet on mobile, modal on desktop. The dialog fetches the most recent notification log entry for the record via `getLastLogisticsNotification`, displays a cooldown warning ("Last sent X ago via Email/WhatsApp"), lets the user pick a single channel (Email with address, WhatsApp with phone number), then calls `resendLogisticsNotification` to resend via the existing `resendNotification` infrastructure. Returns `no_prior_notification` if no prior log exists for the selected channel. Ops, Coordinator, and Super Admin roles can resend; Read-only is forbidden. Server actions isolated in `src/lib/actions/logistics-notifications.ts` (separate from notifications.ts to avoid DB import pollution in pre-existing notification tests).
- Files changed:
  - src/lib/actions/logistics-notifications.ts (new — getLastLogisticsNotification, resendLogisticsNotification server actions)
  - src/lib/actions/logistics-resend.test.ts (new — 11 tests for both server actions)
  - src/app/(app)/events/[eventId]/travel/resend-notification-dialog.tsx (new — ResendNotificationDialog client component)
  - src/app/(app)/events/[eventId]/travel/resend-notification-dialog.test.ts (new — 9 spec-derived tests)
  - src/app/(app)/events/[eventId]/travel/travel-list-client.tsx (added RowActionsMenu, ResendNotificationDialog wiring)
  - src/app/(app)/events/[eventId]/travel/travel-list-client.test.tsx (added logistics-notifications mock to fix DB init)
  - src/app/(app)/events/[eventId]/travel/travel-list-interactions.test.tsx (added logistics-notifications mock)
  - src/app/(app)/events/[eventId]/accommodation/resend-notification-dialog.tsx (new — copy of travel dialog, imports from logistics-notifications)
  - src/app/(app)/events/[eventId]/accommodation/accommodation-list-client.tsx (added RowActionsMenu, ResendNotificationDialog wiring to table and card views)
  - src/app/(app)/events/[eventId]/accommodation/accommodation-list-client.test.tsx (added logistics-notifications mock)
- Tests run: `npm run test:run` — 20 new tests pass (11 action + 9 dialog); 3 existing test files (19+14+14 tests) remain green; 6 pre-existing failing files unchanged (certificate-issuance, circuit-breaker, archive-bypass, travel-cascade, full-journey, mutation-kill-2).
- Risks/follow-ups: `no_prior_notification` path shows an error message in the dialog — there is no fallback "send fresh notification" path since `resendNotification` requires an existing log entry. If no cascade has ever run for a travel/accommodation record, Ops cannot initiate the first send from this UI. Transport resend UI deferred per spec (no template exists yet).

## 2026-04-18T05:10:00Z — PKT-C-004 — Conflict fix CTA destination state

- Story ID: p2-m30-conflict-fix-action
- Implemented: Added orange "Fix" CTA Link to the conflict banner in `schedule-grid-client.tsx` — navigates to `/events/[eventId]/sessions/[sessionIds[1]]?conflict=true`. Updated `sessions/[sessionId]/page.tsx` to accept `searchParams`, resolve `conflict=true`, and pass `conflictMode` to `SessionFormClient`. Extended `SessionFormClient` with `conflictMode?: boolean` prop: (1) mobile alert banner (`lg:hidden`) at top, (2) desktop side-by-side grid (`lg:grid-cols-[280px_1fr]`) with a read-only conflict warning card on the left, (3) warning ring (`ring-2 ring-warning`) on the Date+Time field wrapper with `data-testid="conflict-time-field"`, (4) warning ring and orange section header on the Assignments section with `data-testid="conflict-faculty-field"`, (5) post-save redirect to `/events/[eventId]/schedule` instead of `/sessions` when `conflictMode`.
- Files changed:
  - src/app/(app)/events/[eventId]/schedule/schedule-grid-client.tsx (added Fix CTA Link to conflict banner)
  - src/app/(app)/events/[eventId]/schedule/schedule-grid-conflict.test.tsx (new — 6 tests: Fix CTA presence, URL, missing second sessionId, banner message, +N more, no banner)
  - src/app/(app)/events/[eventId]/sessions/[sessionId]/page.tsx (added searchParams type + conflictMode extraction + prop pass)
  - src/app/(app)/events/[eventId]/sessions/session-form-client.tsx (conflictMode prop, mobile alert, desktop conflict card, field highlights, schedule redirect)
  - src/app/(app)/events/[eventId]/sessions/session-form-client.test.tsx (added 7 conflict mode tests)
- Tests run: `npm run test:run` — 21 new tests pass (6 + 7 + 8 existing); pre-existing failures unchanged (certificate-issuance, circuit-breaker, archive-bypass, travel-cascade, full-journey).
- Risks/follow-ups: Desktop conflict card shows a generic warning message — it does not fetch the conflicting session's data (URL only carries `?conflict=true`, not the conflicting sessionId). If the team wants to show the conflicting session's title/time in the card, a future packet should extend the URL with `?conflictWith=[id]` and fetch it server-side. The assignments section warning ring only applies when assignments exist (edit mode, `assignments.length > 0`).

## 2026-04-17T23:55:00Z — PKT-C-005 — Notification drawer

- Story ID: p6-d8-notification-drawer
- Implemented: Added three server actions to `dashboard.ts` — `getRecentNotifications` (joins `notificationLog` with `people` for name, returns up to 20 items + full unread count), `getNotificationUnreadCount` (lightweight badge count), and `markAllNotificationsRead` (bulk-updates non-read/non-failed rows to read). Created `NotificationDrawer` client component in `src/app/(app)/dashboard/` — bottom sheet on mobile (<768px) and 360px right-side drawer on desktop, with filter tabs (All/Unread/Failed), orange unread dots, channel icons, relative timestamps, status-colored text, and "View all notifications" footer link. Updated `DashboardClient` to wire the bell button to open the drawer, fetch unread count on event select, and display the badge count.
- Files changed:
  - src/lib/actions/dashboard.ts (added RecentNotificationItem type, getRecentNotifications, getNotificationUnreadCount, markAllNotificationsRead; added people + desc/not imports)
  - src/app/(app)/dashboard/notification-drawer.tsx (new — client drawer component)
  - src/app/(app)/dashboard/notification-drawer.test.ts (new — 11 tests, all pass)
  - src/app/(app)/dashboard/dashboard-client.tsx (bell badge + drawer wiring)
- Tests run: `npm run test:run` — 11 new tests pass; 3 pre-existing failing test files (archive-bypass, travel-cascade, full-journey), unchanged.
- Risks/follow-ups: Unread badge pre-loads on event select via `getNotificationUnreadCount`; drawer refreshes on open (fetch-on-open pattern). Individual mark-as-read deferred per spec. Real-time push updates deferred per spec. The "View all notifications" link points to `/events/[eventId]/communications` which has no page yet (future packet).

## 2026-04-17T23:40:00Z — PKT-C-001 — Terms and privacy pages

- Story ID: p2-d7-terms-privacy-page
- Implemented: Created `src/app/(public)/terms/page.tsx` — client component at `/terms` under the existing `(public)` layout. Tab toggle (Terms of Service / Privacy Policy) uses local `useState`. Each tab renders accordion sections (`AccordionSection` component, also local `useState` per item with chevron animation). Static content arrays (`TERMS_SECTIONS`, `PRIVACY_SECTIONS`) each have 6 sections. "Back to Registration" ghost button at the bottom. Added `/terms` link footer to registration form (`registration-form-client.tsx` in the event registration flow).
- Files changed:
  - src/app/(public)/terms/page.tsx (new — full terms/privacy page with tab toggle and accordions)
  - src/app/(public)/terms/terms.test.ts (new — 6 tests: section counts, non-empty title/body, unique titles)
  - src/app/(public)/e/[eventSlug]/register/registration-form-client.tsx (added "Terms & Privacy Policy" footer link)
- Tests run: `npm run test:run` — 6 new tests pass; 3 pre-existing failing test files (archive-bypass, travel-cascade, full-journey), unchanged.
- Risks/follow-ups: Static MDX upgrade (CMS editor) deferred to v2 per design decisions. "Back to Registration" links to `/` (generic) since there is no single registration URL independent of eventSlug — a future packet may pass the event slug via query param if needed.

## 2026-04-17T23:34:30Z — PKT-A-005 — Registration cancellation cascade creates linked red flags

- Story ID: p3-registration-cancel-cascade
- Implemented: Added `REGISTRATION_CANCELLED` cascade event (`conference/registration.cancelled`) to `events.ts` and `emit.ts`. Extended `updateRegistrationStatus` in `registration.ts` to emit the event (with `personId` added to the pre-update select) when `newStatus === 'cancelled'`. Added `handleRegistrationCancelled` to `registration-cascade.ts` — queries active accommodation records and transport passenger assignments for the person within event scope, calls `upsertRedFlag` with `registration_cancelled` flag type on each. No downstream records are deleted.
- Files changed:
  - src/lib/cascade/events.ts (added RegistrationCancelledPayload type, REGISTRATION_CANCELLED event, CASCADE_DIRECTION entry)
  - src/lib/cascade/emit.ts (added registrationCancelledPayloadSchema, registered in productionPayloadSchemas, extended eventId cross-check)
  - src/lib/actions/registration.ts (added personId to select, imported emitCascadeEvent + CASCADE_EVENTS, emit on cancelled transition)
  - src/lib/cascade/handlers/registration-cascade.ts (added handleRegistrationCancelled, registered handler)
  - src/lib/cascade/handlers/registration-cancel-cascade.test.ts (new — 7 tests, all pass)
  - src/lib/actions/registration-cancel-emit.test.ts (new — 3 tests, all pass)
- Tests run: `npm run test:run` — 10 new tests pass; 3 pre-existing failing test files (archive-bypass, travel-cascade, full-journey), unchanged.
- Risks/follow-ups: Transport batches (`transport_batch` target type) are not flagged directly — only passenger assignments are flagged. If a batch-level flag is needed, a future packet should extend the handler. Travel records have no `TARGET_ENTITY_TYPE` entry, so they are not directly flagged; ops can infer from the accommodation/transport flags.

## 2026-04-17T23:30:00Z — PKT-A-004 — Faculty acceptance creates or confirms registration

- Story ID: p2-faculty-accept-creates-registration
- Implemented: Extended `updateFacultyInviteStatus` in `program.ts` to create or confirm a faculty registration when `newStatus === 'accepted'`. On acceptance: (1) fetch event for slug, (2) check for existing registration, (3) if none exists create a new `confirmed` faculty registration with generated registration number and QR token, (4) if a `pending` registration exists update it to `confirmed`, (5) ensure `event_people` link via upsert. Non-accept transitions (opened, declined) are unaffected.
- Files changed:
  - src/lib/actions/program.ts (added `eventRegistrations`, `events` to schema imports; added `generateRegistrationNumber`, `generateQrToken` from validations/registration; extended accept branch in `updateFacultyInviteStatus`)
  - src/lib/actions/program.faculty-accept.test.ts (new — 8 tests, all pass)
- Tests run: `npm run test:run` — 8 new tests pass; 3 pre-existing failing test files (archive-bypass, travel-cascade, full-journey), unchanged.
- Risks/follow-ups: Re-accept with a `cancelled` registration leaves it cancelled (not re-created); edge case deferred. Registration number sequence is not atomic (count-based) — collision risk under concurrent accepts is pre-existing pattern from public registration flow.

## 2026-04-17T23:20:00Z — PKT-A-003 — Per-event registration settings UI

- Story ID: p2-registration-settings-ui
- Implemented: `registrationSettingsSchema` (Zod) in validations; `updateRegistrationSettings` server action (validates eventId, asserts write access, validates input, persists registrationSettings JSONB, revalidates paths); `/events/[eventId]/registration-settings` server page; `RegistrationSettingsClient` form with approvalRequired, maxCapacity, waitlistEnabled, cutoffDate, preferenceFields (dietaryNeeds, travelPreferences, accessibilityRequirements); "Reg Settings" tile added to event workspace PEOPLE & REGISTRATION section.
- Files changed:
  - src/lib/validations/event.ts (added registrationSettingsSchema, RegistrationSettings type)
  - src/lib/actions/registration-settings.ts (new)
  - src/lib/actions/registration-settings.test.ts (new — 16 tests, all pass)
  - src/app/(app)/events/[eventId]/registration-settings/page.tsx (new)
  - src/app/(app)/events/[eventId]/registration-settings/registration-settings-client.tsx (new)
  - src/app/(app)/events/[eventId]/event-workspace-client.tsx (added Reg Settings tile + SlidersHorizontal icon)
- Tests run: `npm run test:run` — 16 new tests pass; 3 pre-existing failing test files (archive-bypass, travel-cascade, full-journey), unchanged.
- Risks/follow-ups: cutoffDate enforcement on public registration form deferred (stored correctly, not yet enforced in form). preferenceFields visibility not yet consumed by public registration form — stored correctly for future use.

## 2026-04-17T23:15:40Z — PKT-A-002 — Event settings page and updateEvent action

- Story ID: p1-event-edit-settings
- Implemented: `updateEventSchema` alias in validations; `updateEvent` server action (validates eventId, asserts write access, validates input, persists name/description/dates/venue/moduleToggles, revalidates paths); `/events/[eventId]/settings` server page (loads event, passes canWrite flag); `EventSettingsClient` form component with field-level error display, module toggle checkboxes, disabled state for read-only users.
- Files changed:
  - src/lib/validations/event.ts (added updateEventSchema, UpdateEventInput)
  - src/lib/actions/event.ts (added updateEvent action, UpdateEventResult type)
  - src/lib/actions/event-settings.test.ts (new — 16 tests, all pass)
  - src/app/(app)/events/[eventId]/settings/page.tsx (new)
  - src/app/(app)/events/[eventId]/settings/event-settings-client.tsx (new)
- Tests run: `npm run test:run` — 16 new tests pass; 2 pre-existing failures in unrelated files (archive-bypass, travel-cascade), unchanged.
- Risks/follow-ups: Settings page is accessible to all write roles (SUPER_ADMIN, EVENT_COORDINATOR, OPS) per assertEventAccess; ops users can currently edit event settings — a future packet may restrict settings to super_admin/coordinator only if needed.

## 2026-04-17T23:10:50Z — PKT-A-001 — Per-event user assignment UI and actions

- Story ID: p2-per-event-user-assignment
- Implemented: `getEventAssignments`, `createEventAssignment` (upsert), `deactivateEventAssignment` server actions; validation schema; `/events/[eventId]/team` page + client; "Team" tile in event workspace ADMIN section.
- Files changed:
  - src/lib/validations/event-assignments.ts (new)
  - src/lib/actions/event-assignments.ts (new)
  - src/lib/actions/event-assignments.test.ts (new — 18 tests, all pass)
  - src/app/(app)/events/[eventId]/team/page.tsx (new)
  - src/app/(app)/events/[eventId]/team/event-team-client.tsx (new)
  - src/app/(app)/events/[eventId]/event-workspace-client.tsx (modified — Team tile)
- Tests run: `npm run test:run` — 18 new tests pass, pre-existing failures unchanged (2 files / 8 tests in travel-cascade, unrelated).
- Risks/follow-ups: UI shows "Team" tile to all roles but page redirects non-super-admins; role-based tile hiding deferred to a future packet.
