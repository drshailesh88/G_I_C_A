// REGRESSION TESTS — eventid-scoping
// Grows as bugs are found and fixed. Each test here came from a real bug.
// Never delete these.

import { test, expect } from '@playwright/test';

test.describe('eventid-scoping — Regressions', () => {
  // (empty — will be populated by anneal and ralph-loop-qa as bugs are found)
});
